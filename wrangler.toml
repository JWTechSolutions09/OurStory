# Cloudflare Pages (opcional). Build: npm run build → dist/
# Variables del proyecto: SUPABASE_URL, SUPABASE_ANON_KEY (como en .env.example)
name = "ourbook"
compatibility_date = "2024-11-01"
pages_build_output_dir = "dist"
